[
  {
    "endpoint": "POST /api/admin/pricing-rules",
    "method": "POST",
    "auth": "ADMIN (Bearer token)",
    "description": "Create a pricing rule for a plan. gst is a percentage (0-100). Effective range optional.",
    "body": {
      "planId": 3,
      "baseRiskRate": 0.0180,
      "processingFee": 450.00,
      "gst": 18.00,
      "effectiveFrom": "2026-08-03T00:00:00",
      "effectiveTo": null,
      "remarks": "Demo pricing rule."
    }
  },
  {
    "endpoint": "PUT /api/admin/pricing-rules/{ruleId}",
    "method": "PUT",
    "auth": "ADMIN (Bearer token)",
    "description": "Update a pricing rule (seeded ruleId 1-6).",
    "body": {
      "planId": 3,
      "baseRiskRate": 0.0170,
      "processingFee": 450.00,
      "gst": 18.00,
      "effectiveFrom": "2026-08-03T00:00:00",
      "effectiveTo": null,
      "remarks": "Updated demo pricing rule."
    }
  },
  {
    "endpoint": "PATCH /api/admin/pricing-rules/{ruleId}/activate",
    "method": "PATCH",
    "auth": "ADMIN (Bearer token)",
    "description": "Activate a pricing rule. No body.",
    "body": null
  },
  {
    "endpoint": "PATCH /api/admin/pricing-rules/{ruleId}/deactivate",
    "method": "PATCH",
    "auth": "ADMIN (Bearer token)",
    "description": "Deactivate a pricing rule. No body.",
    "body": null
  },
  {
    "endpoint": "GET /api/admin/pricing-rules/plan/{planId}/active",
    "method": "GET",
    "auth": "ADMIN (Bearer token)",
    "description": "Get the active pricing rule for a plan (seeded planId 1-6 -> one rule each). No body.",
    "body": null
  },
  {
    "endpoint": "POST /api/admin/pricing-rules/preview",
    "method": "POST",
    "auth": "ADMIN (Bearer token)",
    "description": "Preview premium for a plan without creating a quote. NOTE: this takes productId + pricingRuleId (not planId).",
    "body": {
      "productId": 2,
      "coverageAmount": 500000.00,
      "duration": 2,
      "premiumType": "ANNUAL",
      "pricingRuleId": 3
    }
  }
]
